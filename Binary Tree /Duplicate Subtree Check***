question link :

class Solution {

    HashMap<String, Integer> map = new HashMap<>();
    boolean found = false;

    private String solve(Node root) {
        if (root == null) {
            return "$";
        }

        String left = solve(root.left);
        String right = solve(root.right);

        String curr = root.data + "," + left + "," + right;

        // Ignore leaf nodes
        if (root.left != null || root.right != null) {
            map.put(curr, map.getOrDefault(curr, 0) + 1);

            if (map.get(curr) > 1) {
                found = true;
            }
        }

        return curr;
    }

    public boolean dupSub(Node root) {
        solve(root);
        return found;
    }
}
