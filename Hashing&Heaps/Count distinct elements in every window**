ques link : https://www.geeksforgeeks.org/problems/count-distinct-elements-in-every-window/1

class Solution {
    ArrayList<Integer> countDistinct(int arr[], int k) {
        // code here
        ArrayList<Integer> res = new ArrayList<>();
        HashMap<Integer,Integer> hm = new HashMap<>();
        
        for(int i =0;i<k;i++){
            if(hm.containsKey(arr[i])){
            hm.put(arr[i],hm.get(arr[i])+1);
            }else{
                hm.put(arr[i],1);
            }
        }
        res.add(hm.size());
        for(int i= k;i<arr.length;i++){
            int out = arr[i-k];
            
             hm.put(out, hm.get(out) - 1);
            if(hm.get(out)==0){
                hm.remove(out);
            }
            
             if(hm.containsKey(arr[i])){
            hm.put(arr[i],hm.get(arr[i])+1);
            }else{
                hm.put(arr[i],1);
            }
            res.add(hm.size());
            
        }
         return res;
         
    }
}
