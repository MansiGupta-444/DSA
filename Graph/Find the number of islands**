ques link : https://www.geeksforgeeks.org/problems/find-the-number-of-islands/1

class Solution {
    public void nof(char[][] grid,int i,int j ,boolean vis[][]) {
        
        vis[i][j] =true;
        
       int dx[] = {-1,1,0,0,-1,-1,1,1};
       int dy[] = {0,0,1,-1,-1,1,-1,1};
        
        for(int k=0;k<dx.length;k++){
            int x = i+dx[k];
            int y = j+dy[k];
             if(x >= 0 && y >= 0 && x < grid.length && y < grid[0].length &&
               !vis[x][y] && grid[x][y] == 'L')
              {
                nof(grid, x, y, vis);
              }    
        }
    }
    
    public int countIslands(char[][] grid) {

         int n = grid.length; 
         boolean vis[][] = new boolean[n][grid[0].length];
         int count =0;
         for(int i =0;i<grid.length;i++){
             for(int j =0;j<grid[i].length;j++){
                 if(grid[i][j]=='L' && !vis[i][j]){
                     nof(grid,i,j,vis);
                     count=count+1;
                 }
             }
         }
         
         return count;  
    }
}
